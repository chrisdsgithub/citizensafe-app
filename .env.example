# Firebase Configuration (for backend server)
# This file shows what environment variables are needed for Railway deployment
# Do NOT commit actual secrets - only use on Railway dashboard

# Firebase Service Account (JSON as string)
# Get this from Firebase Console > Project Settings > Service Accounts
# Copy the entire JSON and set it as FIREBASE_SERVICE_ACCOUNT_KEY
FIREBASE_SERVICE_ACCOUNT_KEY='{"type":"service_account","project_id":"citizensafe-437b0",...}'

# Google Cloud Project ID
GOOGLE_CLOUD_PROJECT_ID=citizensafe-437b0

# Gemini API Key (for AI crime prediction)
# Get from Google Cloud Console > APIs & Services > Credentials
GEMINI_API_KEY=your-gemini-api-key-here

# Development Flags
DEV_SKIP_AUTH=false
MOCK_MODE=false

# Flask Configuration
FLASK_ENV=production
PORT=8080
